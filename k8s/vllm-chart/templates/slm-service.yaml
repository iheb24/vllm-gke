{{- if .Values.slm.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: slm-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: slm-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
{{- end }}
